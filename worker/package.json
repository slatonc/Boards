{
  "name": "fortheboards-fulfillment",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe -> Lulu Print API fulfillment webhook for For The Boards",
  "scripts": {
    "test": "node test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail --env production"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
